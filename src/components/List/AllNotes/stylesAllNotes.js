import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
    pinMarker: {
        color: 'gray',
        fontSize: '11px',
        marginLeft: '40px',
        marginBottom: '10px'
    },
    othersMarker: {
        color: 'gray',
        fontSize: '11px',
        marginLeft: '40px',
        marginBottom: '10px',
        marginTop: '60px'
    }
}))

export default useStyles