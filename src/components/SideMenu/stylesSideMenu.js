import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
    menuItem: {
        color: "black",
        padding: "16px",
        borderRadius: '10px 35px 35px 10px'
    }
}))

export default useStyles